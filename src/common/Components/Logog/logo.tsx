import tmdbLogo from '../../../assets/images/logo.svg'

const Logo = () => {
    return (

        <div>
            <img width={150} src={tmdbLogo} alt="TMDB logo"/>
        </div>

    );
};

export default Logo;